<p><%= link_to catalog_item.title, portfolio_show_path(catalog_item) %></p>
<p><%= catalog_item.subtitle %></p>
<p><%= catalog_item.body%></p>
<p><%= image_tag catalog_item.thumb_image unless catalog_item.thumb_image.nil? %></p>
<p><%#= image_tag item.main_image unless item.main_image.nil? %></p>
<%=link_to "Edit", edit_catalog_path(catalog_item) if logged_in?(:site_admin) %>
<%= link_to 'Delete Portfolio Item', catalog_path(catalog_item) , method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)
%>
