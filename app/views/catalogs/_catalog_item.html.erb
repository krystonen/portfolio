<div class="col-md-4">
  <div class="card mb-4 shadow-sm">
    <%= image_tag catalog_item.thumb_image, class:"card-img-top" unless catalog_item.thumb_image.nil? %>
    <div class="card-body">
      <p class="card-text">
        <span><%= link_to catalog_item.title, portfolio_show_path(catalog_item) %></span> <%= catalog_item.subtitle %></p>
    </div>
  </div>
</div>


<!-- <p><%= catalog_item.body%></p> -->
<!-- <p><%= image_tag catalog_item.thumb_image unless catalog_item.thumb_image.nil? %></p> -->
<%#= image_tag item.main_image unless item.main_image.nil? %>
<%#=link_to "Edit", edit_catalog_path(catalog_item) if logged_in?(:site_admin) %>
<%#= link_to 'Delete Portfolio Item', catalog_path(catalog_item) , method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin)
%>
